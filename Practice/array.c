#include <stdio.h>
#include <stdlib.h>

/*int main()
{
    int m[5];
    int i,loc;
    for(i=0;i<5;i++)
    scanf("%d",&m[i]);
    int min=m[0];
  //  printf("%d ",min);
    
    for(i=0;i<5;i++){
        if(min>=m[i]){
        	min=m[i];
        	loc=i;
		}
  		
    }
    printf("lowest marks of roll no %d=%d",loc+1,min);
}*/

int main(){
	char name[50];
	gets(name);
	puts(name);
	
}
